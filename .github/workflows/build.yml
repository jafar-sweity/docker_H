name: Build and Publish Docker Image

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2



      - name: Build and tag Docker image
        run: |
          docker build -t your-dockerhub-username/nginx-static-website:latest .
          docker tag your-dockerhub-username/nginx-static-website:latest your-dockerhub-username/nginx-static-website:${{ github.sha }}

      - name: Push Docker images to Docker Hub
        run: |
          docker push your-dockerhub-username/nginx-static-website:latest
          docker push your-dockerhub-username/nginx-static-website:${{ github.sha }}
